{{#if session.isAuthenticated}}
  <button {{action "invalidateSession"}}>Logout</button>
{{else}}
  <LinkTo @route="login">Login</LinkTo>
{{/if}}

<LinkTo @route="statement-upload">Upload a statement to visualize</LinkTo>
{{outlet}}
