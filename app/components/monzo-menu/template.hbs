Client Id {{model.profile.clientId}}
User Id {{model.profile.userId}}

{{account-switcher
  selectedAccount=this.session.data.account
  accounts=accounts
  onChangeAccount=(action "redirectToAccount")
}}
